@model IEnumerable<string>

<button class="btn btn-primary" type="button" data-bs-toggle="collapse" data-bs-target="#brand" aria-expanded="false" aria-controls="brand">
    Бренды:
</button>

<div class="collapse multi-collapse" id="brand">
    <div class="card card-body">
        @foreach (string brand in Model)
        {
            <a class="btn btn-block
			@(brand == ViewBag.SelectedBrand ? "btn-primary" : "btn-seconadry")"
               asp-action="Catalog"
               asp-controller="Catalog"
               asp-route-brand="@brand"
               asp-route-productPage="1">
                @brand
            </a>
        }
    </div>
</div>

