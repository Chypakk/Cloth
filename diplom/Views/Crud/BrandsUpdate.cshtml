@model Brand

@{
    Layout = "_AdminLayout";
}
<h1>Изменение бренда</h1>
<div class="row">
    <div class="col-auto">
        <form asp-action="BrandsUpdate" method="post">

            <input type="hidden" asp-for="@Model.Id" />

            <label>Имя</label>
            <input class="form-control" asp-for="@Model.Name" />

            <button type="submit" class="btn btn-outline-dark mt-2">Отправить</button>
        </form>
    </div>
</div>
